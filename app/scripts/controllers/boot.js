'use strict'

angular.module('WordStreak')
.controller('BootController', function($scope, $state)	{

	/* check user token is on browser or not. if exist redirect to home view
	or else redirect to login form. */

});